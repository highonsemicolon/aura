syntax = "proto3";
package greeter;

option go_package = "/greeter";

service GreeterService {
  rpc SayHello (SayHelloRequest) returns (HelloResponse);
}

message SayHelloRequest {
  string name = 1;
}

message HelloResponse {
  string message = 1;
}
